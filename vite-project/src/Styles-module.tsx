import CSS from "csstype";

const allStyles = {
  listStylus: {
    margin: "20px 0px",
    width: "100%",
    padding: "0px",
  } as CSS.Properties,
  buttonStyle: {
    width: "150px",
  } as CSS.Properties,
};

export default allStyles;
